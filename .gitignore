# Workspaces are a local choice; the adapters resolve the root through a
# replace directive, so nothing here needs one.
go.work
go.work.sum

# `go build ./...` inside a main package directory drops the executable there.
/cmd/codemode-mcp/codemode-mcp
/cmd/codemode-mcp/internal/fixtureserver/fixtureserver

*.test
*.out
.idea/
.vscode/
.DS_Store
